export namespace ModelsAuth{
    export interface DatosRegistro{
        email: string;
        password: string;
    }

        export interface DatosLogin{
        email: string;
        password: string;
    }
}